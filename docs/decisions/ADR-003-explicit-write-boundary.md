# ADR-003: explicit write boundary

Decision: local processing has no network by default; SharePoint writes require an explicit UI action and metadata declaration. Reason: upload, authorization, and privacy claims must remain inspectable.
