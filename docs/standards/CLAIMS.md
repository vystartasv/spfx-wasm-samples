# Claims

`VERIFIED` is supported by repository tests or named controlled evidence. `EXPECTED` is a design expectation. `EXPERIMENTAL` needs deployment or measurement. `NOT SUPPORTED` is an explicit boundary. Never promote a dependency/vendor statement to repository evidence.
