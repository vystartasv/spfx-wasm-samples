# Accessibility

Use localized visible labels, semantic controls, keyboard operation, stable headings, `aria-live` status for async work, clear errors, disabled/cancel states, and text alternatives for result tables. Do not describe a sample as accessible without checking the rendered path.
