# Sample contract

Every sample has README, source, tests, localized UI, `sample.yml`, limits, and third-party attribution. Metadata is JSON-compatible YAML and is validated by `scripts/validate-metadata.mjs` against [sample.schema.json](sample.schema.json); the generated root matrix is not hand-maintained.
