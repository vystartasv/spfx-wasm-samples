# Architecture principles

Evidence before advocacy; native baseline before WASM; worker by default for expensive/stateful work; explicit network/write boundary; bounded input and cancellation; typed contracts; visible fallback and failure; accessibility/localization; and no claim beyond evidence.
