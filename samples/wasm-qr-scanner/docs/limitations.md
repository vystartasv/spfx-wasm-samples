# Limitations

- Still images only; camera permission and live-stream lifecycle are deliberately deferred.
- Browser-native detection has no measured elapsed time here, so it is a baseline rather than a benchmark.
- ZBar supports the symbologies documented by its package, subject to image quality and browser WASM/CSP policy.
- Local processing describes this sample's code boundary, not host telemetry or administrator-managed instrumentation.
