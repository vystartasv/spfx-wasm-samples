# Benchmarks

No benchmark numbers are prefilled. Every result displays the worker’s `performance.now()` duration, bytes scanned, duplicate bytes, groups, and measured engine. Use the deterministic 10,000-record fixture to compare browser sessions; hardware, browser, tab contention, and Web Crypto implementation affect results.
