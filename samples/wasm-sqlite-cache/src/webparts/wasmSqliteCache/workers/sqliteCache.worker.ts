import { MemoryStore } from '../../../core/memory-store';
import { openCacheStore, Sqlite, SqliteStore } from '../../../core/sqlite-store';
import { CONTRACT_VERSION, isValidRpcRequest, Namespace, RpcResponse, validateMutationPayload } from '../../../core/types';
type Store = SqliteStore | MemoryStore;
const scope = self as unknown as { onmessage: ((event: MessageEvent<{ version: number; id: string; method: string; payload?: unknown }>) => void) | null; postMessage: (message: RpcResponse) => void };
let store: Store | undefined;
const errorShape = (error: unknown): { code: string; message: string; retryable: boolean } => ({ code: (error as { code?: string })?.code || 'CACHE_ERROR', message: error instanceof Error ? error.message : String(error), retryable: false });
function requireStore(): Store { if (!store) throw Object.assign(new Error('Initialize the cache before using it.'), { code: 'NOT_INITIALIZED' }); return store; }
async function init(namespace: Namespace): Promise<unknown> { if (!namespace?.tenantId || !namespace.userObjectId || !namespace.applicationId) throw Object.assign(new Error('tenantId, userObjectId, and applicationId are required.'), { code: 'INVALID_NAMESPACE' }); try { const sqlite = await (await import(/* webpackChunkName: 'sqlite-wasm' */ '@sqlite.org/sqlite-wasm')).default(); store = await openCacheStore(sqlite as unknown as Sqlite, namespace); return store.status(); } catch (error) { store = new MemoryStore(namespace, `SQLite could not initialize: ${error instanceof Error ? error.message : String(error)}. Using an in-memory fallback; persistence is not verified.`); return store.status(); } }
async function handle(request: { version: number; method: string; payload?: unknown }): Promise<unknown> { if (request.version !== CONTRACT_VERSION) throw Object.assign(new Error(`Unsupported RPC version ${request.version}.`), { code: 'UNSUPPORTED_VERSION' }); if (request.method === 'init') return init(request.payload as Namespace); const active = requireStore(); const payload = (request.payload || {}) as { title?: string; category?: 'Red' | 'Blue' | 'Green'; amount?: number; id?: string; patch?: Record<string, unknown>; mode?: 'success' | 'failure' | 'conflict' }; if (['add', 'update', 'delete'].indexOf(request.method) >= 0) validateMutationPayload(request.method, payload); switch (request.method) { case 'status': return active.status(); case 'hydrate': active.hydrate(); return active.status(); case 'query': return active.query(payload); case 'add': active.add(payload.title!, payload.category!, payload.amount!); return active.status(); case 'update': active.update(payload.id!, payload.patch!); return active.status(); case 'delete': active.remove(payload.id!); return active.status(); case 'sync': return active.sync(payload.mode || 'success'); case 'clear': active.clear(); return active.status(); default: throw Object.assign(new Error(`Unknown cache operation: ${request.method}.`), { code: 'UNKNOWN_METHOD' }); } }
scope.onmessage = event => { const request = event.data; const usable = !!request && typeof request === 'object' && typeof request.id === 'string' ? request.id : undefined; if (!isValidRpcRequest(request)) { if (usable) scope.postMessage({ version: CONTRACT_VERSION, id: usable, ok: false, error: errorShape(Object.assign(new Error('Malformed worker request.'), { code: 'BAD_REQUEST' })) }); return; } handle(request).then(result => scope.postMessage({ version: CONTRACT_VERSION, id: request.id, ok: true, result })).catch(error => scope.postMessage({ version: CONTRACT_VERSION, id: request.id, ok: false, error: errorShape(error) })); };
